<!--
Thanks for the contribution. Keep this short — the diff shows what changed,
so use this space for what the diff cannot show.
-->

## What this changes

<!-- One or two sentences. -->

## Why

<!-- The reason, if it is not obvious from the change itself. Link an issue
     with "Closes #123" if there is one. -->

## How it was verified

<!-- What you actually ran or clicked through. "pnpm test" alone is fine if
     that is genuinely all it needed. -->

## Anything left out

<!-- Known gaps, follow-ups, or workarounds. Say so here rather than leaving
     them to be discovered in review. Write "nothing" if there are none. -->

---

- [ ] `pnpm type-check`, `pnpm lint`, `pnpm test`, and `pnpm build` all pass
- [ ] Organization-scoped queries filter by `organizationId`
- [ ] Schema changes include a committed migration
- [ ] Docs updated if behaviour someone reads about has changed
